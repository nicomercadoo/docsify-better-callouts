{
  "name": "docsify-better-callouts",
  "version": "0.1.0",
  "description": "A Docsify pluggin to impove (and customize) the default callouts.",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
      "build": "bun build src/main.js --outdir=dist --minify",
      "build:dev": "bun build src/main.js --outdir=dist --sourcemap",
      "build:watch": "bun build src/main.js --outdir=dist --sourcemap --watch"
  },
  "keywords": [
    "docsify",
    "docsify-plugin",
    "markdown"
  ],
  "author": "Nicolás Mercado",
  "license": "GPL-3.0",
  "devDependencies": {
  }
}
